# Changelog

为避免发布流程向受保护的 `main` 分支写回提交，本文件不由发布任务自动改写。每个版本的完整变更记录由 semantic-release 生成并保存在 GitHub Releases 中。

## Unreleased

- 精简 A2UI 审批与问答布局：单题直接展示问题，选项说明并入可选项，删除重复标题和操作提示；卡片标题与会话摘要分离，等待态更新不覆盖问题，保留原始授权范围及回调答案。

- 数字员工提供绑定会话的宿主恢复路由，支持 Web 与 IM 共用连接器持有的 Agent；并发恢复去重、失败时保留原会话，归属冲突或连接器失败会回复明确提示。旧 DSH Web 入口需配合接入此路由。
- A2UI 明确区分等待审批、选择、输入、提交和失效状态；本地 DWS 适配携带摘要组件注解并串行更新状态，避免统一显示处理中或空产物提示；会话列表实际文案仍需客户端验收。
- 数字员工工具预设缺失或挂载失败时停止本轮并回复明确配置错误，不再使用空工具集继续请求模型。
- 优化 A2UI 卡片的 Markdown 标题、说明与分组；审批/问答结束后保留只读请求和答案回执，不再只显示一句状态文字。
- 新增独立于数字员工的 A2UI approve / ask 通用交互模块；兼容原子卡片 `a2uiEvent.action.context` 回调，并按显式 UID 或 OpenDingTalkId 身份空间校验服务端操作人。默认运行时尚未接线。
